Observation: Claude likes talking about "gotchas", judging by Handout A Session
2 and the result of running `/init` on my ancient steam boiler project.

Session 7:

> `two_player_mode()` always uses `starting_player='X'` (it doesn't alternate
> across games — a small deviation from SPECS.md §4.3). Worth a flag if you want
> me to fix it.
